# 一、训练效率指标

### `Learning iteration 282/500000`

当前是第 282 次 policy update，总计划 500000 次。

> ⚠ 你现在还在极早期阶段（0.05%），任何震荡都很正常。

---

### `Computation: 34164 steps/s`

每秒环境仿真步数。

* **collection: 0.756s** → rollout采样时间
* **learning: 2.121s** → 反向传播更新时间

评估标准：

| 指标                       | 判断               |
| ------------------------ | ---------------- |
| steps/s 越高越好             | 取决于GPU/并行env数    |
| learning 时间 > collection | 正常（AMP 有额外判别器训练） |

---

# 二、PPO核心损失

## 1️⃣ `Value function loss: 10.4454`

价值网络 MSE：

[
L_v = (V(s) - R)^2
]

评估标准：

| 数值   | 说明    |
| ---- | ----- |
| < 1  | 非常好   |
| 1–10 | 正常    |
| > 20 | 可能不稳定 |

你现在 **10.4** → 偏高但可接受（早期阶段）

⚠ 若持续升高 → critic 崩溃

---

## 2️⃣ `Surrogate loss: 0.0077`

PPO clipped objective。

[
L^{CLIP}
]

评估标准：

| 现象   | 含义    |
| ---- | ----- |
| 接近0  | 更新幅度小 |
| 大幅波动 | 学习率过大 |
| 恒为0  | 学不动   |

你这个 **0.0077** → 很健康。

---

# 三、AMP相关指标（关键）

AMP核心是：

[
\min_\pi \max_D
]

---

## 3️⃣ `AMP loss: 0.0663`

判别器 loss。

* 过高 → 判别器没学会
* 过低 → 判别器太强

理想范围：

| 范围       | 状态     |
| -------- | ------ |
| 0.05–0.2 | 理想     |
| <0.01    | 判别器过拟合 |
| >0.5     | 判别器太弱  |

你现在：**健康**

---

## 4️⃣ `AMP grad pen loss: 0.1425`

Gradient penalty（WGAN-GP 风格）

保持在：

* 0.1 ~ 0.5 → 正常

你现在正常。

---

## 5️⃣ 判别器输出均值（极重要）

```
AMP mean policy pred: -0.7905
AMP mean expert pred: 0.7856
```

这是判别器输出：

* expert 应接近 +1
* policy 应接近 -1

理想情况：

| expert | policy | 说明      |
| ------ | ------ | ------- |
| +1     | -1     | 判别器区分明显 |
| +1     | +1     | 模仿成功    |
| 0      | 0      | 判别器崩溃   |

你现在：

* expert ≈ +0.78
* policy ≈ -0.79

说明：

✔ 判别器非常健康
✔ 还没有成功模仿（policy 仍明显不同）

早期非常正常。

---

# 四、Adaptation Module

## `Adaptation module loss: 0.0052`

通常用于 domain randomization 或感知适应。

评估：

* 越接近 0 越好
* 不应爆炸

你这里完全正常。

---

# 五、探索相关

## `Mean action noise std: 0.68`

动作高斯噪声标准差。

评估：

| 阶段 | 正常范围    |
| -- | ------- |
| 初期 | 0.5–1.0 |
| 后期 | 0.1–0.3 |

你现在 0.68 → 探索阶段，很合理。

---

# 六、核心性能指标（最重要）

## 🎯 `Mean reward: 255.75`

最关键指标。

评估方法：

* 看趋势，而不是绝对值
* 是否持续上升

如果：

* 平滑上升 → 正常学习
* 震荡但上升 → 正常
* 长期不涨 → reward设计问题

---

## `Mean episode length: 226.73`

评估：

* 是否接近 max_episode_length？
* 是否逐渐增加？

如果最大步数是 300：

* 226 → 说明已经能稳定跑

如果最大是 1000：

* 说明还经常 early termination

---

# 七、奖励分解分析（关键诊断）

## `rew_tracking_lin_vel: 70.6852`

线速度跟踪奖励

✔ 很高 → 任务目标在实现

---

## `rew_tracking_ang_vel: 14.4677`

角速度跟踪

✔ 正常

---

## `rew_action_rate: -8.2441`

动作变化惩罚

负值说明：

* 动作变化较剧烈

如果越来越负 → 关节抖动严重

---

## `rew_action_smoothness: -0.0643`

平滑惩罚

值很小 → 很健康

---

## `rew_torques: -0.0182`

扭矩惩罚

几乎0 → 很好（说明动作不暴力）

---

## `rew_base_height: -0.0000`

高度稳定 → 没摔

---

# 八、整体诊断

在 iteration 282：

✔ 判别器健康
✔ PPO稳定
✔ reward合理
✔ 没有崩溃迹象
✔ 还处于早期

属于 **非常健康的训练状态**

---

# 九、真正的评估标准（工程级）

训练是否成功看这四个：

### 1️⃣ reward 曲线是否单调上升（滑动平均）

### 2️⃣ policy_pred 是否从 -0.8 → 0 → +0.8

这是AMP是否成功的关键。

---

### 3️⃣ episode length 是否接近 max length

---

### 4️⃣ action noise std 是否自动下降

---

# 十、如果出现异常该怎么判断？

| 现象                  | 原因           |
| ------------------- | ------------ |
| value loss 爆炸       | critic 学习率太大 |
| AMP expert=policy≈0 | 判别器崩溃        |
| reward 不涨           | 奖励权重问题       |
| episode length 突然下降 | 学坏了          |

---

# 十一、针对你现在的情况建议

你现在不需要调参。

等到：

* iteration > 5000
* 再看趋势

